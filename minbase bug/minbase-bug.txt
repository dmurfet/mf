// Bug report
link l = "MPfile: ./idempotent.mp";
read(l);
def e = read(l);
module im = minbase(e);
2 * y(2) * im[5] + 2 * y(3) * im[5] + im[4] == im[2];
// The columns of im should be a minimal generating set, but this is false.