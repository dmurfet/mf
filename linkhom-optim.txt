// Optimisation tests

option(noredefine);option(noloadLib);option(redSB);//option(prot);
LIB "blow.lib";
LIB "linkhom.lib";
LIB "matrix.lib";
LIB "control.lib";
LIB "mfweb.lib";
ring rr=0,(x(1..16)),dp;
printlevel = 1;
int N = 3;

list linkL = linkLibrary("8_19");
list web = webFromLink(linkL, intvec(1,1,0,1,0,0,0,0), N);
list h = webCompile(web, defaultCompStratForWeb(web));

// Baseline config: [mfPushforwardInductive] total elapsed time 153070ms. 
// [webCompile] Entering round 5 of compilation.

// Sanity checks off: [mfPushforwardInductive] total elapsed time 127910ms.
// [webCompile] Entering round 5 of compilation.